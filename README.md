# Database backup tool

```
# db_backup.py --help
usage: db_backup.py [-h] [-c file] [-o directory]

Database weekly rolling backup script. Supports MySQL and PostgreSQL.

optional arguments:
  -h, --help    show this help message and exit
  -c file       Configuration file (default: backup.ini)
  -o directory  Output directory (default: .)
```
